# Vanilla spawn selection, but a starter zone is still configured at (0,0).
# Expect: player spawns wherever vanilla's own search picks for this seed
# (likely NOT plains); the plains starter zone still exists at (0,0)
# regardless — walk there to confirm it's still forced, just not where you
# started.
starter:
  biome: 'minecraft:plains'
  radius: 256
spawn:
  strategy: vanilla_spawn
